<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clinica_fisioterapeutica">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Controllers.Fichas.FiltroActivity"></activity>
        <activity android:name=".Controllers.Turnos.AgregarReservaActivity" />
        <activity android:name=".Controllers.Turnos.EditarReservaActivity" />
        <activity android:name=".Controllers.Fichas.FichaArchivoActivity" />
        <activity android:name=".Controllers.Fichas.FichaActivity" />
        <activity android:name=".Controllers.Fichas.AgregarEditarFichaActivity" />
        <activity android:name=".Controllers.FichaArchivoActivity" />
        <activity android:name=".Controllers.Turnos.TurnosActivity" />
        <activity android:name=".Controllers.Fichas.FichasActivity" />
        <activity android:name=".Controllers.MenuActivity" />
        <activity android:name=".Controllers.Paciente.AgregarEditarPersonaActivity" />
        <activity android:name=".Controllers.Paciente.PacientesActivity" />
        <activity android:name=".Controllers.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>